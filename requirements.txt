# Development / runtime environment for the ruleofthumb package.
# Install from this directory:
#   pip install -r requirements.txt && pip install -e ".[dev]"
#
# NOTE: inputs must be rectangular batches; see ToDo.md for the
# mixed-length limitation preserved from the original research code.

# base (minimal install: pip install ruleofthumb-rot)
numpy==2.2.6
torch==2.13.0

# text extra (pip install ruleofthumb-rot[text])
transformers==5.15.1

# image extra (pip install ruleofthumb-rot[image])
torchvision==0.28.0
pillow==12.3.0
captum==0.9.0

# plot extra (pip install ruleofthumb-rot[plot])
matplotlib==3.10.9
wordcloud==1.9.6
shap==0.49.1
shap-editorial==0.1.2

# dev tooling (integration tests only — never imported by the package)
scikit-learn==1.7.2
pandas==2.3.3
pytest==9.1.1
ruff==0.16.4
build==1.5.0
nbclient==0.11.0
jupyter==1.1.1

# docs site only (pip install -e ".[docs]"; see docs-src/ + PUBLISH.md)
sphinx==8.1.3
myst-nb==1.4.0
pydata-sphinx-theme==0.19.0
sphinx-copybutton==0.5.2
